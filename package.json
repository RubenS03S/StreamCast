{
  "name": "streamcast",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "StreamCast — partage d'écran WebRTC PC → iPad / iPhone",
  "scripts": {
    "dev": "node server/dev.mjs"
  },
  "engines": {
    "node": ">=22.12.0"
  },
  "dependencies": {
    "@netlify/blobs": "^11.1.1"
  },
  "devDependencies": {
    "@netlify/functions": "^6.0.0"
  }
}
